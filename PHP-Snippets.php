// Dump and die
dd($var);

// Ambil IP user
$_SERVER['REMOTE_ADDR'];

// Cek string mengandung substring
if (strpos($haystack, $needle) !== false) {}

// JSON encode + decode
$data = json_decode($json, true);
$json = json_encode($array);
